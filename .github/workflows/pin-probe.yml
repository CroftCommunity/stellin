# THROWAWAY: does sha_pinning_required actually refuse a floating tag?
name: pin-probe
on: [pull_request]
permissions:
  contents: read
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "a floating tag ran"
